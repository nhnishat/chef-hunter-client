import React, { useContext } from 'react';
export AuthContext=useContext(null)
const AuthProvider = () => {
    return (
        <div>
            
        </div>
    );
};

export default AuthProvider;